apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mikrotik-backup.fullname" . }}-test-connection"
  labels:
    {{- include "mikrotik-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mikrotik-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
